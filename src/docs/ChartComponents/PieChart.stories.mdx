import { PieChart } from '@/components'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'

<Meta
  title="Documentation/Chart Components/PieChart"
  component={PieChart}
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true } } }}
/>

# PieChart

Used to create a pie chart.

Please refer to [amChart's documentation](https://www.amcharts.com/docs/v4/reference/piechart/) on pie charts for more details.

## Params

Below are some useful parameters for configuring a `PieChart` component.

<Props of={PieChart} />
