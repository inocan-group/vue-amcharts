import { WordCloud } from '@/components'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'

<Meta
  title="Documentation/Chart Components/WordCloud"
  component={WordCloud}
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true } } }}
/>

# WordCloud

"Word clouds help visualize weight or importance of individual words from a keyword list or text."

Please refer to [amChart's documentation](https://www.amcharts.com/docs/v4/chart-types/wordcloud/) on word clouds for more details.

## Params

Below are some useful parameters for configuring a `WordCloud` component.

<Props of={WordCloud} />
