import { MapChart } from '@/components'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'

<Meta
  title="Documentation/Chart Components/MapChart"
  component={MapChart}
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true } } }}
/>

# MapChart

Used to create maps.

Please refer to [amChart's documentation](https://www.amcharts.com/docs/v4/chart-types/map/) on map charts for more details.

## Params

Below are some useful parameters for configuring a `MapChart` component.

<Props of={MapChart} />
