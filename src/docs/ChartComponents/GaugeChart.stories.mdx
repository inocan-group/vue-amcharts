import { GaugeChart } from '@/components'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'

<Meta
  title="Documentation/Chart Components/GaugeChart"
  component={GaugeChart}
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true } } }}
/>

# GaugeChart

"Gauge chart is used to display all kinds of one-dimensional progressive values."

Please refer to [amChart's documentation](https://www.amcharts.com/docs/v4/chart-types/gauge-chart/) on gauge charts for more details.

## Params

Below are some useful parameters for configuring a `GaugeChart` component.

<Props of={GaugeChart} />
