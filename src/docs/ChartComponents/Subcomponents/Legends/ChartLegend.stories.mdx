import { ChartLegend } from '@/components'
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks'

<Meta
  title="Documentation/Chart Components/Subcomponents/Legends/ChartLegend"
  component={ChartLegend}
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true } } }}
/>

# ChartLegend

A `ChartLegend` component can be used in the default slot of many chart components to create legends.

Please refer to [amChart's documentation](https://www.amcharts.com/docs/v4/concepts/legend/) on legends for more details.

## Params

Below are some useful parameters for configuring a `ChartLegend` component.

<Props of={ChartLegend} />
